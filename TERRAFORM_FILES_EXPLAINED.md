# Terraform Files Generated During Execution

## Current File Structure in Each Run Workspace

When you create a Terraform run, the system generates a workspace directory (e.g., `runs/run_20260208_184750/`) containing:

### ✅ Currently Generated Files (6 files)

| File | Purpose | Generated By |
|------|---------|--------------|
| **main.tf** | Main Terraform configuration | LLM Generator |
| **variables.tf** | Variable declarations | LLM Generator |
| **outputs.tf** | Output definitions | LLM Generator |
| **request.json** | Original user request metadata | Backend API |
| **state.json** | Run state tracking (status, timestamps) | Backend API |
| **terraform.log** | Terraform CLI output logs | Terraform Runner |

### ❌ Missing Terraform Files (Should Be Generated)

When Terraform actually runs (not in MOCK mode), these additional files **should** be created:

| File/Directory | Purpose | Generated By | When |
|----------------|---------|--------------|------|
| **.terraform/** | Provider plugins & modules | `terraform init` | Always |
| **.terraform.lock.hcl** | Provider version lock file | `terraform init` | Always |
| **tfplan** | Binary execution plan | `terraform plan -out=tfplan` | Before apply |
| **terraform.tfstate** | Current infrastructure state | `terraform apply` | After successful apply |
| **terraform.tfstate.backup** | Previous state backup | `terraform apply` | After 2nd+ apply |

## Why These Files Are Missing

Looking at your logs, I see:
```
INFO | [MOCK] Simulating terraform init success
```

**Your system is running in MOCK MODE**, which means:
- ✅ The three `.tf` files are generated by the LLM
- ✅ Logs are written
- ❌ Terraform CLI is NOT actually executed
- ❌ No `.terraform/`, `tfplan`, or `terraform.tfstate` files are created

## How to Enable Real Terraform Execution

### Option 1: Disable Mock Mode in Code

Edit `app/core/config.py`:
```python
# Current (MOCK mode)
MOCK_MODE = os.getenv("MOCK_MODE", "true").lower() == "true"

# Change to (REAL mode)
MOCK_MODE = os.getenv("MOCK_MODE", "false").lower() == "true"
```

### Option 2: Set Environment Variable

In your `.env` file or environment:
```bash
MOCK_MODE=false
```

### Option 3: Check Current Config

Let me check what's currently set in your config...

## What Happens in Real Mode

When MOCK_MODE is disabled and Terraform actually runs:

### 1. After `terraform init`
```
runs/run_XXXXXX/
├── main.tf
├── variables.tf
├── outputs.tf
├── .terraform/              ← NEW
│   └── providers/
│       └── registry.terraform.io/
│           └── hashicorp/
│               └── aws/
│                   └── 5.x.x/
│                       └── windows_amd64/
│                           └── terraform-provider-aws.exe
└── .terraform.lock.hcl      ← NEW
```

### 2. After `terraform plan`
```
runs/run_XXXXXX/
├── ... (all previous files)
└── tfplan                   ← NEW (binary file)
```

### 3. After `terraform apply`
```
runs/run_XXXXXX/
├── ... (all previous files)
├── terraform.tfstate        ← NEW (JSON state)
└── terraform.tfstate.backup ← NEW (after 2nd apply)
```

## State File Contents

The `terraform.tfstate` file is a JSON file that tracks:
- **Resources created**: IDs, ARNs, attributes
- **Dependencies**: Resource relationships
- **Provider configuration**: Versions, regions
- **Outputs**: Computed values

Example structure:
```json
{
  "version": 4,
  "terraform_version": "1.5.0",
  "serial": 1,
  "lineage": "unique-id",
  "outputs": {
    "instance_id": {
      "value": "i-1234567890abcdef0",
      "type": "string"
    }
  },
  "resources": [
    {
      "mode": "managed",
      "type": "aws_instance",
      "name": "web",
      "provider": "provider[\"registry.terraform.io/hashicorp/aws\"]",
      "instances": [...]
    }
  ]
}
```

## Security & State Management

### Current Approach (Good for Development)
- ✅ Each run has its own isolated workspace
- ✅ State files are kept locally in `runs/` directory
- ✅ Easy to inspect and debug
- ❌ State files are not backed up
- ❌ No state locking for concurrent runs
- ❌ State contains sensitive data (passwords, keys)

### Production Recommendations

For production, you should implement:

1. **Remote State Backend** (S3 + DynamoDB or GCS)
   ```hcl
   terraform {
     backend "s3" {
       bucket         = "terraform-state-bucket"
       key            = "runs/${run_id}/terraform.tfstate"
       region         = "us-east-1"
       encrypt        = true
       dynamodb_table = "terraform-locks"
     }
   }
   ```

2. **State Encryption**
   - Encrypt state files at rest
   - Use KMS/Cloud KMS for encryption keys

3. **State Locking**
   - Prevent concurrent modifications
   - Use DynamoDB (AWS) or Cloud Storage (GCP)

4. **State Versioning**
   - Enable versioning on S3/GCS bucket
   - Keep history of state changes

## Next Steps

1. **Check Current Mode**: Let me verify if MOCK_MODE is enabled
2. **Test Real Execution**: Try a simple run with real Terraform
3. **Verify File Generation**: Confirm all files are created
4. **Implement State Backend**: Add remote state for production

Would you like me to:
- Check your current MOCK_MODE setting?
- Help you set up real Terraform execution?
- Implement a remote state backend?
